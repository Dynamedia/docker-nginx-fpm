version: "3.4"

services:
  nginx-fpm:
    build: .
    image: dynamedia/docker-nginx-fpm:v1.19.6_8.0.1
    env_file:
      - .env
    restart: always
    ports:
      - 80:80
    # Get a geoIP2/GeoLite2 database file and name it geoip.mmdb - place it in this directory and uncomment below
    #volumes:
    #  - ./geoip.mmdb/:/etc/nginx/geoip.mmdb
